# ─── DoorLoop API ────────────────────────────────
DOORLOOP_API_KEY=your_doorloop_api_key_here
DOORLOOP_BASE_URL=https://api.doorloop.com/v1

# ─── OpenAI ──────────────────────────────────────
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o

# ─── Telegram Bot ────────────────────────────────
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_from_botfather
# Comma-separated Telegram user IDs allowed to use the bot (landlord/managers only)
TELEGRAM_ALLOWED_USER_IDS=123456789,987654321
# Set to true on Vercel (webhook mode); false / omit for local long-polling
TELEGRAM_WEBHOOK_MODE=false
# Optional: random secret verified on every /webhooks/telegram request
TELEGRAM_WEBHOOK_SECRET=your_random_secret_here

# ─── Webhook Server ──────────────────────────────
WEBHOOK_PORT=3000
# Secret used to validate incoming webhook signatures from DoorLoop
WEBHOOK_SECRET=your_webhook_shared_secret
# Publicly reachable URL for this server (ngrok / production domain)
WEBHOOK_BASE_URL=https://your-server.example.com

# ─── SMS (Twilio – optional; falls back to DoorLoop native SMS) ──
USE_TWILIO=false
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_FROM_NUMBER=+15550001234

# ─── Reports ─────────────────────────────────────
# Where to deliver weekly reports: "telegram" | "email" | "gdrive"
REPORT_DELIVERY=telegram
REPORT_GOOGLE_DRIVE_FOLDER_ID=
REPORT_EMAIL_TO=landlord@example.com

# ─── Vercel Cron ─────────────────────────────────
# Auto-set by Vercel; forward as "Authorization: Bearer <secret>" on cron requests
# Set manually here only if you want to protect /cron/* outside of Vercel
CRON_SECRET=

# ─── Logging ─────────────────────────────────────
LOG_LEVEL=info
